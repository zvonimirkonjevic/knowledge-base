[project]
name = "sqlalchemy-sandbox"
version = "0.1.0"
description = "Collection of small projects created during learning of SQLAlchemy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg2>=2.9.12",
    "sqlalchemy>=2.0.50",
]
